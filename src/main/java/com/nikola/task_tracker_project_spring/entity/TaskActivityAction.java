package com.nikola.task_tracker_project_spring.entity;

// See TaskStatus: enum value meanings are documented on the referencing field (TaskActivity.action).
public enum TaskActivityAction {
    CREATE,
    UPDATE,
    DELETE
}
